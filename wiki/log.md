# Termiti Wiki — Log

> Append-only log of all operations. Add entries as `## [YYYY-MM-DD] type | description`.
> Quick scan of recent entries: every entry starts with `## [`.

---

## [2026-05-21] init | Wiki created

Initial wiki structure created for Termiti game following the Karpathy LLM Wiki pattern.

**Pages created:**
- `CLAUDE.md` — LLM agent schema
- `index.md` — page catalog
- `overview.md` — CZ game overview
- `architecture.md` — technical architecture
- `cards/types.md` — card types
- `cards/effects.md` — CardEffect sealed class
- `cards/decisions.md` — Decision cards
- `mechanics/game-flow.md` — turn flow
- `mechanics/resources.md` — resource system
- `mechanics/mines.md` — mine system
- `mechanics/combo.md` — Combo system
- `mechanics/win-conditions.md` — win conditions
- `systems/ai.md` — AiEngine
- `systems/online.md` — online multiplayer

**Knowledge base built from:**
- Full development conversation (Lottie removal, Decision bugs, cards 113–117, rarity changes, starter deck, 99-round limit)
- Source files: CardEffect.kt, Gameviewmodel.kt, GameSession.js, cards.js, AiEngine.kt

## [2026-05-21] ingest | Card 117 — Průzkum dolů

New card `117 Průzkum dolů` added (1 MAGIC, Combo, EPIC) with `DecisionMine` effect.
Updated pages: `cards/decisions.md`, `mechanics/mines.md`, `systems/ai.md`, `cards/effects.md`.

## [2026-05-26] ingest | Cards 119–121, C39 + new effects

New cards added (4 ATTACK Combo EPIC `119 Válečný pokřik`, 2 MAGIC EPIC `120 Hromadná sleva`, 3 CHAOS RARE `121 Kletba cen`, C39 CHAOS EPIC `Velký zmatek`).

New effects introduced: `GiveRandomCard(costType)`, `ModifyHandCost(delta, targetOpponent)`, `RandomizeHands`.

Updated pages: `cards/effects.md`.

## [2026-05-26] ingest | Card 122 Stavební pokřik + win-condition fix

New card `122 Stavební pokřik` (4 STONES, Combo, EPIC): `bc(7)` + `grc('STONES')` — direct building mirror of `119 Válečný pokřik`.

Win condition fix in `GameState.kt`: `currentTurn > 99` (was `>= 99`); both-decks-empty path now always uses `resolveByHp()` even when turn limit fires; castle/death conditions evaluated before turn limit.

Top bar texture replaced: `bg_top_bar.png` (1920×160 px) replaces `bg_top_panel.png`.

Updated pages: `cards/effects.md`, `log.md`.

## [2026-05-28] ingest | Cards 124–128 (new + cost-6 magic)

New cards added:
- `124 Alchymistova volba` (2 MAGIC, Combo, RARE): `DecisionChooseResource` — pick 4 Magic / 4 Attack / 4 Stones.
- `125 Válečný zápal` (2 ATTACK, Combo, RARE): `ap(4)` + `grp(CHAOS,1,'Útok')`.
- `126 Magický proud` (3 MAGIC, Combo, EPIC): `grp(CHAOS,2,'Magie')`.
- `127 Pyroblast` (6 MAGIC, EPIC): `ac(10)` + `dr(MAGIC,2)` — direct castle hit + magic drain.
- `128 Archmág` (6 MAGIC, Combo, LEGENDARY): `dpc()` + `grp(MAGIC,1)` — this turn, each extra card played draws 1 and gains 1 Magic (dual engine).

New effect introduced: `DecisionChooseResource(options)` — decision overlay shows resource buttons instead of cards; resolved via `resolveResourceDecision` (offline) / `resolveOnlineResourceDecision` (online). Server: added to `DECISION_TYPES`, `_buildDecisionOptions`, `_resolveDecision`; `DECISION_REQUEST` carries `resourceOptions`.

127/128 use only existing effects (`AttackCastle`, `DrainResource`, `DrawPerCardPlayed`, `GainResourcePerCardPlayed`).

Updated pages: `cards/effects.md`, `cards/decisions.md`, `log.md`.

## [2026-05-29] ingest | v0.1.0 — localization, versioning, Temný mág, AI lethal lookahead

Large upstream pull (`30baa61..123c54a`, 44 files). Wiki synced to reflect:

- **Localization system landed** (was only "prepared"): UI strings + all 176 card texts + passive abilities localizable via `assets/lang/<code>.json`; complete `cs` + `en`; Czech fallback chain. New types `LanguagePack`, `CardText`, `AppStrings`, `LanguageManager`. → **new page `systems/localization.md`**.
- **Versioning introduced**: SemVer + `CHANGELOG.md` + git tag `v0.1.0`, `versionCode`/`versionName`, and `PROTOCOL_VERSION` client↔server handshake (`VERSION_MISMATCH` on mismatch). → `architecture.md`, `systems/online.md`.
- **App display name** renamed Termiti → **Darkmage**. → `overview.md`, `architecture.md`.
- **New card `L01` Temný mág** (13 CHAOS, LEGENDARY): `sr` MAGIC/ATTACK/STONES 3 each + `ac(18)` + `sca(5)` — iconic namesake legendary. Card total now **176**.
- **`DecisionChooseResource` reworked**: options now render as **placeholder cards** (`resourcePlaceholderCard()`) reusing the card-picker UI, not custom buttons; localized titles. → `cards/decisions.md`, `cards/effects.md`.
- **AI**: documented `isLethal` + combo-chain lethal lookahead (`comboSetupForLethal`, ported to simulator), smarter discard, DecisionChooseResource picks least-held resource. → `systems/ai.md`.
- **`syncCards` now regenerates `cards.json` at build** (no more manual sync) — corrected the stale "Node.js not in PATH" workaround. → `architecture.md`.
- **Card balance**: `125 Válečný zápal` chaos trigger now counts **every** card (`grp('CHAOS',1)`, no type filter); Temný mág cost 8→13.

Updated pages: `overview.md`, `architecture.md`, `systems/online.md`, `systems/ai.md`, `cards/decisions.md`, `cards/effects.md`, `index.md` + new `systems/localization.md`.

## [2026-05-21] maintenance | Translated wiki to English

## [2026-05-21] ingest | Localization infrastructure

Added language switching (Czech / English) preparation:
- `Language.kt` — enum with CZECH / ENGLISH + label + flag
- `LanguageManager.kt` — singleton, SharedPreferences persistence, `currentState: MutableState<Language>` for reactive Compose updates
- `AppStrings.kt` — `AppStrings` data class with all UI strings + `CzStrings` + `EnStrings` + `LocalStrings` CompositionLocal + `currentStrings` shortcut
- `CardPresentation.kt` — `CardPres.descriptionEn` field added; `localizedDescription(language)` helper
- `SettingsScreen.kt` — language toggle (CZ 🇨🇿 / EN 🇬🇧) + strings via `LocalStrings`
- `MainActivity.kt` — `LanguageManager.init()` + `CompositionLocalProvider(LocalStrings provides ...)`

Migration status: SettingsScreen strings migrated. All other screens still use hardcoded Czech strings — migrate by replacing literals with `LocalStrings.current.*`.

All pages rewritten in English for token efficiency. `overview.md` kept in Czech as a human-readable overview.
## [2026-07-12] ingest | AI discard fix: discards only with a full hand + non-empty deck (previously discarded even with an empty deck = pure card loss); Discard rules section added to systems/ai.md
## [2026-07-12] ingest | Mirror/Clone cost parity: online server charges Klon as source effectiveCost+1 in source costType (was: own cost 2 + modifier); ModifyHandCost costModifier preserved on morph cards (online client no longer hardcodes it, offline updateCloneCards keeps curse idempotently); Morph cards section added to cards/effects.md
## [2026-07-12] ingest | Online empty-deck fix: SKIP_TURN from a player who played/discarded this turn (combo) is treated as normal end-turn via actedThisTurn tracking; game no longer ends prematurely. Documented in mechanics/win-conditions.md
## [2026-07-12] ingest | Discard rule change: discarding a card no longer ends the turn, allowed 1x per turn (offline GameViewModel.playerDiscardUsed + online GameSession.discardUsedThisTurn, server-authoritative with myState.discardUsed for the client). Documented in mechanics/game-flow.md
## [2026-07-12] ingest | AI endgame last-chance fallback fix: force-played zero-effect conditional cards (e.g. Zasobnik with unmet condition) when losing with empty decks. realizedAttackOrBuild() now recurses into ConditionalEffect + checkCondition; absolute fallback now requires effectScore > 0. Documented in systems/ai.md
## [2026-07-16] ingest | Online: self-inflicted overdraw burns (natural turn-draw with full hand, DrawCard/DrawBoth cards like Studna vedomosti, RandomizeHands) were logged but not shown in the discard slot - GAME_STATE's stale lastPlayedCard (only reset in _handleEndTurn/_handleSkipTurn, not after _advanceTurn's non-combo path) overwrote the CARD_LOST-driven display. Fixed in _advanceTurn (burned.length gate, was traps-only) and _handlePlayCard (selfLostCards gate). Documented in systems/online.md
## [2026-07-16] ingest | Online: Likvidace (DecisionBurnOpponent) / Zakerny spion (PeekAndStealHand) left the discard slot permanently stuck showing the lost opponent card instead of reverting to 'played [card]', because _resolveDecision unconditionally nulled lastPlayedCard, preventing the natural self-correction that direct BurnCard/StealCard plays get for free. Fixed: skip the null for these two decision types + client-side gameLog dedup by card instance id (lastLoggedPlayedCardId) to avoid duplicate log entries from the resend. Documented in systems/online.md
## [2026-07-16] ingest | server.js had NO WebSocket heartbeat at all - a connection that dies without a clean TCP close (WiFi drop, OS-suspended app) stays readyState=OPEN on the server indefinitely, becoming a permanent ghost in players/queue/superQueue. Matches reported bug: two devices in lobby saw different online counts, and both searching super_random never matched each other. Added standard ws-library ping/pong heartbeat (30s interval, ws.terminate() on missed pong, reuses existing 'close' cleanup path). Documented in systems/online.md
## [2026-07-16] ingest | AI: Inspirace (DrawPerCardPlayed cardType="Magie") played with no effect - AI's totoKoloPenalty/totoBuff gated on comboCardsInHand (any isCombo card in hand), but the real trigger (GameViewModel._playCard) checks card.type against the effect's cardType filter, unrelated to isCombo. AI overvalued Inspirace when holding unrelated combo cards (played it alone, nothing to trigger it) and undervalued/misordered it against non-combo Magie cards (played those first, Inspirace last = wasted). Fixed: matchingTypeCount()/typeMatches() replace the combo-count check in DrawPerCardPlayed/GainResourcePerCardPlayed/GainCastlePerCardPlayed scoring + totoKoloPenalty + totoBuff; added waitForSetupPenalty to discourage playing a matching payoff card before its still-unplayed setup card. Documented in systems/ai.md
## [2026-07-17] ingest | Balance: Spion (071) reworked to "Steal 1 of each resource. +2 Chaos." (added ar('CHAOS',2) on top of the existing 4x steal-1). Text cleanup: removed the redundant "vc. chaosu"/"incl. Chaos" clause from Spion, Chaoticka krize (C09), Anarchie (C12), Chaoticky vymenik (C31) descriptions - "kazdeho zdroje"/"each resource" already implies Chaos, no functional change for those three. Updated cards.js, cards.json, CardPresentation.kt, cs.json, en.json, deckbuilder.html.
## [2026-07-17] ingest | Balance: Drak (051) and Upiri drak (078) chaos bonus reduced from +4 to +3 (ar('CHAOS',4) -> ar('CHAOS',3)). Demon (052) chaos +4 left unchanged (not requested). Updated cards.js, cards.json, CardPresentation.kt, cs.json, en.json, deckbuilder.html.
## [2026-07-19] ingest | Fixed offline auto-pass glitch: when the player's hand+deck are both empty, finishTurn() recursively calls itself every round (only AI acting) with just delay(700L) between rounds - felt like a glitch, AI appearing to rapid-fire several identical cards in a row with no breathing room. Added delay(600L) before the recursive finishTurn() call in this branch only (normal turn transitions unaffected). Documented in mechanics/game-flow.md.
## [2026-07-19] ingest | Found the real cause of the "AI plays the same card 3x in a row" + spurious lose-sound-then-win glitch: the auto-pass branch (player hand+deck both empty) set gameState.value with activePlayer=PLAYER before its own delay/recursive finishTurn() call. endPlayerTurn()/waitTurn() guard only on activePlayer==PLAYER, so the player could click during that window and start a SECOND concurrent finishTurn() coroutine racing the queued one - two AI turns played out over separate deep-copied state (interleaved plays looked like duplicated cards) and could independently reach scheduleGameEnd() with different results (stale lose sound fires synchronously, then the correct win result overwrites the pending reveal). Fixed: auto-pass branch now locks activePlayer=AI immediately, matching the pattern already used elsewhere in finishTurn (documented in mechanics/game-flow.md).
## [2026-07-20] ingest | New "Discard" mechanic: Card.discardEffects (List<CardEffect>, default empty) - alternative effect list fired ONLY when a card is discarded instead of played; card.effects never runs for that instance. Reuses existing CardEffect types (no new effect kinds); self-harm designs use negative amounts on self-targeting effects. Wired into all four game paths: offline human discardCard(), offline AI (AiAction.Discard branch in finishTurn), online server _handleDiscardCard() (GameSession.js, with CARD_LOST notifications + win check), and the deckbuilder.html simulator. AiEngine.bestDiscard() gained scoreDiscardEffect() (separate from scoreEffect, correctly signed for self-harm) so the AI avoids discarding cards that would hurt it and prefers discarding ones that would help it. UI: GameCardView.kt shows a skull badge (DiscardRed border) on cards with discardEffects. New log string logDiscardEffectTriggered (cs/en). Infrastructure only - no card uses the mechanic yet (example discussed: "Zamořené krysy", not created). Documented in new page mechanics/discard.md.
## [2026-07-26] ingest | Campaign localization: CampaignLocation/CampaignOpponent had zero i18n (raw hardcoded Czech .name/.title/.description read directly by all 3 campaign screens). Added title field to CardText, campaignLocations/campaignOpponents maps to LanguagePack, matching LanguageManager lookups, and displayName/displayTitle/displayDescription getters (mirroring Card.displayName). Screens switched to display* properties. Full en+cs content for 4 locations + 40 opponents added to lang/*.json. Also backfilled missing en/cs translations for cards "133"-"139" (see below - added same day, translations were skipped). Documented in systems/localization.md.
## [2026-07-26] ingest | 6 new discard-mechanic cards ("133"-"139", skipping none) + new CardEffect ConvertWallToCastle (converts caster's entire current wall HP into castle HP, first used by Legendary "139" Pohlcení hradeb). Two earlier drafts of the STONES self-harm card ("Zazděný poklad", "Vázaný duch") were rejected by design review as strictly-worse duplicates of existing cards "009"/"013" and redesigned around fresh effect combos (DrainResource(STONES) primary, siphon patterns) before final approval as "138" Podkopané valy. New effect wired offline (CardEffect.kt, GameLogic.kt applyEffects, CardRepository.kt JSON parsing, AiEngine.kt scoreEffect, GameViewModel.kt scoreCardForSituation + sound detection, GameCardView.kt icon, DeckBuilderScreen.kt/ArenaDraftScreen.kt category) and online (engine.js applyEffects, GameSession.js smart-pick scoring, cards.js cwtc() helper) - exhaustive-when compiler errors used to find every offline call site. New placeholder card "134" Krysa (TrapOnDraw(AttackWall(3)), isPlaceholder, maxCopies:0) shuffled in by "133" Zamořené krysy, realizing the hypothetical example mechanics/discard.md originally used to motivate the whole mechanic. Not updated: deckbuilder.html SIMULÁTOR tab's separate hand-authored shorthand effect DB (DECK BUILDER tab unaffected, reads real cards.json). Server-side changes not runtime-tested (no Node.js in this environment). Documented in cards/effects.md and mechanics/discard.md.
## [2026-08-26] ingest | "134" redesigned per explicit user spec, replacing the draw-triggered trap with a real playable curse card. Was: Krysa, 0 CHAOS, effects:[TrapOnDraw(AttackWall(3))] (fired automatically on draw, no player choice). Now: Zamořená Krysa, 3 CHAOS, effects:[] (no play effect - playing it purely costs chaos and, being non-combo, ends the turn via the existing default turn-end-after-play rule, no new CardEffect needed), discardEffects:[BuildCastle(-4), BuildWall(-4)]. Net: whoever draws it must choose between burning a turn + 3 chaos for nothing or taking 4 castle + 4 wall via discard. isPlaceholder/maxCopies:0 unchanged - still only enters play via "133"'s AddToOpponentDeck, still excluded from all reward/decision/deck-builder pools. Updated cards.json, CardPresentation.kt, cards.js, cs.json, en.json. Documented in mechanics/discard.md.
## [2026-07-24] ingest | Wired real card art for the 8 discard-mechanic cards "132"-"139" (Zoufaly zold, Zamorene krysy, Zamorena krysa, Osudova mince, Posledni vypad, Zapomenuta poznamka, Podkopani hradeb for "138" Podkopane valy, Pohlceni hradeb), replacing the art_placeholder_* stand-ins in CardPresentation.kt. User supplied the PNGs directly into app/src/main/res/drawable/ (untracked, no "art_" prefix, distinct from the pre-existing "050" Podkopani hradeb's art_podkopani_hradeb.png). No effect/balance changes.
## [2026-08-26] ingest | Balance: "133" Zamorene krysy - removed its own discardEffects ([bw(-5)]) entirely, now [] (no effect if 133 itself is discarded unplayed). Only "134" Zamorena Krysa (the seeded curse card) still punishes on discard (-4 castle, -4 wall) - correctly hits whoever actually holds and discards it (the opponent, since only their deck receives copies via AddToOpponentDeck). "136" Posledni vypad: AttackPlayer amount 7->10, rarity EPIC->RARE (maxCopies unchanged at 2, both rarities map to the same value). Updated cards.js, cards.json, CardPresentation.kt, cs.json, en.json.
## [2026-08-26] ingest | Online: added server-side PLAY_CARD throttle (PLAY_CARD_MIN_INTERVAL_MS=1000) so two combo cards played back-to-back by the same side are processed at least 1s apart - previously a fast double-tap during a combo chain could get processed almost instantly, sending GAME_STATE updates too fast for the opponent's client to render/notice. Implemented as a queueing wrapper (_handlePlayCard reserves the next free per-side slot >= now, >= end of previous reservation, then calls the renamed _playCardNow via setTimeout if needed) rather than outright rejection, so 3+ rapid taps queue up 1s apart instead of colliding. Guards against the game ending/turn changing mid-delay (same activeSide/phase==='playing' check pattern as the existing _turnTimer callbacks). Server-side only (server/game/GameSession.js) - offline has no equivalent issue (single device, no opponent client to outrun).
## [2026-08-26] ingest | Balance/text: "080" Velkovyroba's description said "Kazdy dul +2" ("every mine +2") but the actual effect ([am('MAGIC',2), am('ATTACK',2), am('STONES',2)]) never included Chaos - misleading. Reworded to match "044" Trifekta dolu's pattern ("Dul magie, utoku i kamene +2"), no effect change. Updated CardPresentation.kt, cs.json, en.json.
## [2026-08-26] ingest | Reworked the PLAY_CARD pacing fix from earlier today: the previous approach delayed *processing* of PLAY_CARD itself (server-side queue before _playCardNow ran), which meant the acting player's own combo also felt sluggish/blocked, not just the opponent's view - not what was wanted. Replaced with _sendStateBothPaced(): the acting player's GAME_STATE always sends immediately (their own play stays instant), only the opponent's copy is routed through a new per-side FIFO delivery queue (_queueRevealFor/_pumpRevealQueue) spaced >=1s apart (REVEAL_MIN_INTERVAL_MS), so each combo card still gets its own reveal to the opponent instead of being skipped/coalesced. Added _clearRevealQueue(), called from the plain _sendStateBoth() before every authoritative broadcast (turn end, discard, decision, game end), to prevent a stale queued reveal from arriving after a later "urgent" message and flickering the opponent's UI out of order. Removed the old _nextAllowedPlayAt/PLAY_CARD_MIN_INTERVAL_MS processing-delay mechanism entirely. Documented in systems/online.md.
## [2026-08-28] ingest | Online mulligan: "Čekám na soupeře…" now shows the opponent's remaining decision time. The countdown (`mulliganSecondsLeft`) was cancelled the moment we submitted, so after confirming there was no clock at all. It now keeps running past our own submit — both clients start it from the same server deadline (`timeoutMs/1000 - 2`), so the remaining seconds ARE the opponent's remaining seconds; the auto-confirm at 0 is harmless because `confirmMulligan()` early-returns once submitted. Cancelled instead on `OPPONENT_MULLIGAN_DONE` (then we're waiting on GAME_STATE, not on them) and on confirm/skip when the opponent had already finished. New localized key `mulliganWaitingOpponentTimer` ("Čekám na soupeře… (%d s)" / "Waiting for opponent… (%ds)"); `MulliganOverlay` gained `waitingSecondsLeft`, fed by `OnlineMulliganLayer` as `secondsLeft.takeIf { submitted && !oppDone }`. Offline passes null → unchanged.
## [2026-08-28] ingest | Card text: CardPresentation.kt (the built-in Czech fallback) had drifted from assets/lang/cs.json for 33 of 190 cards. The language pack is what actually renders (every call site uses Card.displayDescription → LanguageManager.cardDesc), so the fallback only surfaces when a pack omits a key — but it had genuinely wrong content: "104" Válečný trénink claimed "[Combo]" (cards.js isCombo=0), "131" Invaze said "Zaútočí za 6" (effect is ap(8)), "124" Alchymistova volba listed 3 of its 4 resource options. Verified the packs against the actual effects in cards.json for every divergence — the packs were right in all 33 cases — then rewrote the CardPresentation strings from cs.json (script-driven, exact match now 0 differences). No effect or balance change; cs.json/en.json untouched.
## [2026-08-28] ingest | Online: ghost effect for cards burned/stolen out of the OPPONENT's hand never rendered (offline it does, via GameViewModel.aiHandLossLog). NewBattlefield consumes an ordered oppLossQueue, one entry per rub removed from the opponent's strip; online passed nothing and the documented "fallback na lastCard" could not work, because CARD_LOST arrives BEFORE the GAME_STATE that shrinks oppState.handSize — by the time LaunchedEffect(aiHandSize) reruns, GAME_STATE has reset lastPlayedAction to PLAYED with the attacker's own card, so the branch bailed out via return@let. Fixed by giving online a real queue: server tags CARD_LOST with fromHand:true where the card genuinely left a hand (direct BurnCard/StealCard in _playCardNow, the same path from discardEffects, PeekAndStealHand), deliberately NOT for DecisionBurnOpponent which burns from the deck (a queued entry there is never consumed and would shift the cursor, showing the wrong card for every later loss). Client adds OnlineLobbyViewModel.opponentHandLoss, filled only on causedByMe + fromHand, reset in resetGameState, passed as oppLossQueue. Additive protocol field, no PROTOCOL_VERSION bump. Server changes not runtime-tested (no Node.js in this environment). Documented in systems/online.md.
## [2026-08-26] ingest | Wired in new user-supplied assets: (1) 10 new castle skins (castle_player_4..13) + brand-new wall skin system (wall_player2..6, PlayerProfile.wallSkin was previously nonexistent - a single hardcoded wall_player.png was used everywhere). Added wallSkinDrawable() resolver (GameBattlefield.kt, mirrors castleSkinDrawable), threaded playerWallResId/opponentWallResId through NewBattlefield/NewCastleStructure/WallBlock, wired into GameScreen.kt (offline) and OnlineGameScreen.kt/OnlineLobbyViewModel.kt/server.js (online JOIN + MATCH_FOUND propagation, KNOWN_WALL_SKINS whitelist, mirrors castleSkin's existing pattern). ProfileScreen.kt: extended CastleSkinPicker to all 13 skins + new WallSkinPicker section, both converted from weight(1f) Row to horizontalScroll (fixed 84dp cells) since 13 items no longer fit un-scrolled. New localized labels: castleOutlawCamp (castle_player_4's distinct skull/wood theme), castleVariant/wallVariant (%d template for the visually-similar numbered variants), wallClassic. (2) Campaign: 10 goblin_*.png character portraits wired as the "Goblinský tábor" location's 10 CampaignOpponent avatars (exact 1:1 name match confirmed against CampaignData.kt); AvatarView resolver whitelist extended in both CampaignLocationScreen.kt and CampaignResultScreen.kt (duplicated function, both updated). goblin_kral_profil (close-up) used for the boss avatar slot; goblin_kral (wide throne scene) has no existing UI slot, left unwired. castle_goblin.png in the user's campaign/goblin/ folder was byte-identical to castle_player_4.png - kept only the flat copy, deleted the duplicate. wall_goblin.png is unique (spiked wood palisade) - wired as the goblin location's opponent battle wall in GameScreen.kt (checks campaignOpponent?.id?.startsWith("gob_")). Files under res/drawable/campaign/goblin/ were flattened to res/drawable/ root (Android does not support subdirectories under res/<type>/ - the nested files would not have compiled at all).
## [2026-08-26] ingest | Fix: goblin campaign portraits were rendered as a small 60dp centered icon in the opponent roster card (CampaignLocationScreen.kt), same treatment as the old enemy_icon_1/2/3 placeholders - wrong, since these are full illustrations meant to fill the card's art window like real game cards do. Extracted avatarDrawableRes() from AvatarView's inline when-block, added FULL_ART_AVATARS set (the 10 goblin_* ids) and a new OpponentCardArt() composable that renders full-bleed (fillMaxWidth/height(artH), ContentScale.Crop) for full-art avatars, falling back to the old small centered AvatarView for icon-style avatars (enemy_icon_N, emoji). CampaignResultScreen.kt's AvatarView (40dp, victory/defeat summary row) was correct as-is and left untouched - that's a genuinely small-icon context, not a card art window.
## [2026-08-26] ingest | Fixed goblin campaign art assignment per user correction: goblin_kral (wide throne scene) is the roster CARD art for the boss, goblin_kral_profil (close-up) is the small in-battle icon (NewTopBar, top-right during a live match) - I had these backwards. Added CampaignOpponent.cardArt: String? = null (falls back to avatar when unset) to let the roster card and the battle icon differ per opponent. All 9 non-king goblins: avatar="player_icon_10" (generic, no individual battle icon exists for them) + cardArt="goblin_X" (their individual full illustration, unchanged from before). gob_king: avatar="goblin_kral_profil" (unchanged) + cardArt="goblin_kral" (new). CampaignLocationScreen.kt's OpponentCardArt call now reads opponent.cardArt ?: opponent.avatar; FULL_ART_AVATARS swapped goblin_kral_profil for goblin_kral; avatarDrawableRes gained goblin_kral + player_icon_10. CampaignResultScreen.kt's local resolver gained player_icon_10 (its small 40dp victory/defeat-screen icon still reads opponent.avatar, unaffected in scope but now needs to resolve the new default). ProfileScreen.kt's central avatarResId (used by AvatarDisplay, called from NewTopBar) gained goblin_kral_profil so the live-battle top-right icon actually renders it instead of falling through to emoji-text. Also: new user-supplied goblin_tabor.png (goblin camp scene) wired as the "loc_goblins" location art in both CampaignLocationScreen.kt and CampaignMapScreen.kt's locationArtRes() - previously borrowed R.drawable.art_goblin (the "046 Goblin" playing card's own art, left untouched since it's still used there).
## [2026-08-26] ingest | Campaign opponent roster rarity was hardcoded to EPIC for every non-boss opponent (LEGENDARY only for the boss) - all purple, no progression. Added opponentRarityRes(order, total, isBoss) in CampaignLocationScreen.kt: splits the non-boss opponents evenly into common/rare/epic thirds by position (e.g. 9 regular opponents -> 1-3 common, 4-6 rare, 7-9 epic), boss always legendary. Threaded a new `total` param through OpponentCard (location.opponents.size). Generic over opponent-count per location, not hardcoded to 10.
## [2026-08-26] ingest | Added battlefield background rotation: 3 new user-supplied backgrounds (castle_background_swamp/vulcan/winter) join the original castle_background in a random pool (randomBattleBackground() in GameBattlefield.kt), picked once per match and held in GameViewModel.battleBackgroundResId / OnlineLobbyViewModel.battleBackgroundResId (set at every "new match" entry point: restartGame, startArenaBattle, roguelike battle start, and MATCH_FOUND/reconnect-recovery online - never re-picked on recomposition). Campaign is exempt from the pool: startCampaignBattle forces castle_background_goblin for "gob_"-prefixed opponents (Goblinsky tabor), default castle_background otherwise (no themed art yet for the other 3 locations). Critical constraint: the 6 torch-flame positions (TorchFlame composable, hardcoded % coordinates) are fitted to castle_background.png's specific torch-sconce pixel positions only - NewBattlefield now takes a backgroundResId param and gates the entire flames/crop-math block on backgroundResId == R.drawable.castle_background, so every other background renders flame-free instead of showing torches floating in the wrong spot. Threaded through GameScreen.kt (offline/WiFi/arena/campaign/roguelike, shared) and OnlineGameScreen.kt.
## [2026-08-26] ingest | Campaign location-selection screens (CampaignMapScreen.kt - pick a location, CampaignLocationScreen.kt - pick an opponent within one) switched their background from menu_bg/bg_game to bg_plain.png per user request.
## [2026-08-26] ingest | ProfileScreen.kt: back button changed from FantasyButton to PlainButton (matching CampaignMapScreen.kt/CampaignLocationScreen.kt's back button style - textColor/fontSize/padding identical) per user request ("divne tlacitko zpet"). Background swapped from deckbuild_bg to bg_plain.png (matching the campaign screens' new background); bg_side_panels (side panel textures) and bg_separator (section dividers) left untouched per explicit instruction.
## [2026-08-26] ingest | Found the actual cause of ProfileScreen's "weird back button": LocalStrings.current.back already equals "<- ZPET" (arrow + all-caps baked into the localized string, cs.json/en.json), and my earlier fix had wrapped it as "<- ${back}" - producing a literal double-arrow all-caps "<- <- ZPET". Changed to a plain hardcoded "Zpet" (Title Case, no arrow), matching what the user asked for and consistent with CampaignMapScreen.kt/CampaignLocationScreen.kt's back button, which also hardcodes its Czech text directly rather than going through the shared (arrow+caps) `back` string.
## [2026-08-26] ingest | Fixed campaign location-selection screens looking almost black: CampaignMapScreen.kt/CampaignLocationScreen.kt both had a 0xBF (~75% opacity) near-black overlay tuned for the OLD background (menu_bg/bg_game). Since switching to bg_plain.png (an already-dark stone-frame texture, see prior entry), the same heavy overlay hid it almost entirely. Reduced both to 0x40 (~25%) - just enough tint for text legibility, background texture now actually visible.
## [2026-08-26] ingest | Fixed the location description text in CampaignLocationScreen.kt's left panel (the "card" showing e.g. "Goblinsky tabor" + its description): (1) it literally overlapped the curved location name - ArcCardName occupies y=110..142dp but the description Box started at y=140dp, a 2dp overlap. Moved to y=145dp (height 54->50dp, keeps the same 3-4dp gap before the "X/Y porazeno" status text at y=198dp). (2) Font bumped 8sp->9sp / lineHeight 11sp->12sp for readability, horizontal padding 12->14dp for a small safety margin against the card frame art. (3) Color was ClMuted (0xFF7A6E5F, a dark muted brown - barely visible against the dark art background); switched to ClText (0xFFEDE0C4, close to the 0xFFDDD0B0 real game cards use for description text in GameCardView.kt).
## [2026-08-26] ingest | Same fix applied to the individual opponent cards (right panel of CampaignLocationScreen.kt, e.g. "Goblin Pruzkumnik") - had the identical bug as the location panel fixed earlier: description Box started at y=140dp while ArcCardName ends at y=142dp (2dp overlap). Moved to y=145dp (height 54->50dp), font 7.5sp->8.5sp / lineHeight 9.5sp->10.5sp, horizontal padding 12->14dp. Color was already Color(0xFFDDD0B0) here (matches real game cards), left unchanged - the "too dark" complaint was specific to the location panel's ClMuted, not this one.
## [2026-08-26] ingest | Opponent card text (CampaignLocationScreen.kt right panel) pushed further down per user feedback to match the location panel's approved position: offset y=145dp->151dp, height 50dp->44dp (kept bottom edge stable so the reward-chips row below still clears the status row at y=198dp).
## [2026-08-26] ingest | Applied the same text fix to CampaignMapScreen.kt's LocationCard (the location-picker cards on the campaign map, e.g. "Goblinsky tabor") - same overlap bug as CampaignLocationScreen.kt: description Box started at y=153dp while ArcCardName ends at y=155dp (offset 120+height 35). Moved to y=158dp (height 40->36dp, keeps bottom edge clear of the progress/status block at y=196dp), font 8sp->9sp, lineHeight 11sp->12sp, padding 12->14dp. Color (CmDesc/CmMuted toggle by unlocked state) was already correct here, left unchanged. Also iterated several small dp nudges on CampaignLocationScreen.kt per user feedback: opponent-card description Box (145->151->154dp offset, height 50->44->41dp), status row "Porazen/Bojuj!" text (198->199->200dp), and the location screen's own back button shifted right (padding start 16dp->28dp).
## [2026-08-26] ingest | Traced the "status text not in the frame" complaint by actually measuring card_frame_attack.png pixels (Python/PIL) instead of guessing dp offsets: the frame's small dark footer tab (meant for the status/type label) sits at ~94-99% of the card's height, not ~90% where the status texts were positioned. Fixed CampaignLocationScreen.kt's location-panel "X/Y porazeno" (offset 200->207dp, height 12dp added, font 10sp->8sp) and opponent-card "Porazen/Bojuj!" (same 200->207dp/12dp, icon 11->9dp, font 9->8sp) - both use a 220dp-tall card, matching the measured fraction directly. Also realized the user's prior "vycisteno/citac" instruction was actually about CampaignMapScreen.kt's LocationCard status ("Vycisteno"/"$defeated / $total"), which uses a taller 240dp card and had NOT been touched (hence "beze zmeny") - moved its Column (progress bar + status row) from offset 196dp to 218dp so the status row (last element) lands inside the proportionally-scaled footer tab (~225-238dp for a 240dp card) instead of higher up in the description-panel area.
## [2026-08-28] fix | Reverted the pixel-measurement-based footer-tab fix per user report ("rozesral jsi v lokaci to porazeno - vrat to"): the ~94-99% card-height fraction derived from card_frame_attack.png pixel sampling was wrong in practice. CampaignLocationScreen.kt's opponent-card "Porazen/Bojuj!/Zamcen" status row reverted to its prior working values: offset 207dp->200dp (height 12dp modifier removed entirely), icon 9dp->11dp, spacer 3dp->4dp, font 8sp->9sp (location-panel "X/Y porazeno" text was already reverted to offset 200dp/font 10sp in the same pass). CampaignMapScreen.kt's LocationCard status Column - confirmed directionally correct (196->218dp was the right direction) but overshot per user feedback ("nesedi, chce to o trochu nahoru") - pulled back up to 208dp as a conservative middle-ground pending further pixel-level user feedback. Lesson: pixel-sampling a frame PNG to infer a text-safe zone does not reliably match how the art actually reads on-screen; iterative dp nudges from user visual feedback remain the trustworthy method for this UI.
## [2026-08-28] fix | CampaignMapScreen.kt LocationCard status Column nudged 208dp->210dp per user feedback ("o 2dp nize").
## [2026-08-28] ingest | Unified CampaignMapScreen.kt's LocationCard description text (the 4 location-picker cards) with CampaignLocationScreen.kt's opponent-card description styling per user request: fontSize 9sp->8.5sp, lineHeight 12sp->10.5sp, unlocked color CmDesc(0xFFCBBFA5)->Color(0xFFDDD0B0) (exact match to the opponent card's fixed color), offset 158dp->167dp (proportionally shifted down to match the opponent card's post-name gap on its taller 240dp card vs the opponent card's 220dp). Locked-location dimming (CmMuted) kept as a distinct branch - the opponent card has no locked/unlocked color split to mirror, and removing the lock cue wasn't requested. CmDesc color constant removed (now unused).
## [2026-08-28] fix | PlayerProfileManager.kt createProfile(): starting gold for a brand-new account raised 300->500 per user request.
## [2026-08-28] fix | Two online-multiplayer bugs fixed per user request: (1) opponent's castle/wall cosmetic skin was only ever sent once, in MATCH_FOUND - if the app was killed/restarted mid-game and matchInfo was lost, the GAME_STATE-reconstruction fallback in OnlineLobbyViewModel.kt (matchInfo.value == null branch) had no way to recover it and silently defaulted to "castle_player"/"wall_player" (wrong skin shown for the rest of the match). Fixed by making GameSession.js (server) actually track each side's castleSkin/wallSkin for the FULL lifetime of the match (constructor params castleSkinA/B, wallSkinA/B, stored as this.castleSkin/this.wallSkin), not just pass them through once at matchmaking time - _buildStateFor() now includes castleSkin/wallSkin on oppStatePayload in every GAME_STATE push, so the client's reconstruction fallback reads the real values from oppState instead of hardcoding defaults. (2) Battle background was picked independently by EACH client (GameBattlefield.kt's randomBattleBackground(), called separately in OnlineLobbyViewModel.kt's MATCH_FOUND handler and its GAME_STATE-reconstruction fallback) - two players in the same online match could see different backgrounds. Fixed by moving the random pick server-side: server.js gained BATTLE_BACKGROUNDS pool + pickBattleBackground() (mirrors GameBattlefield.kt's RANDOM_BATTLE_BACKGROUNDS, no castle_background_goblin since that's campaign-only), picked ONCE per match in the matchmaking block and sent as `background` in both MATCH_FOUND messages AND stored on GameSession (this.background, included in every GAME_STATE payload) for the same reconstruction-after-restart case as the skins. Client: added battleBackgroundDrawable(id: String): Int in GameBattlefield.kt (string->drawable resolver, mirrors castleSkinDrawable/wallSkinDrawable), OnlineLobbyViewModel.kt's MATCH_FOUND and GAME_STATE-reconstruction handlers now call battleBackgroundDrawable(json.optString("background",...)) instead of randomBattleBackground(). Offline/campaign/arena/roguelike are unaffected - they're single-player, so client-side randomBattleBackground() stays correct there; only online needed server authority.
## [2026-08-28] ingest | Wired 6 new user-supplied campaign castle/wall skins (castle_citadela/hory/drak.png, wall_citadela/hory/drak.png) as the AI's battlefield look for the 3 non-goblin campaign locations, matching by location name (Trpasličí hory->hory, Temná citadela->citadela, Dračí impérium->drak). Generalized the mechanism that previously only special-cased "Goblinský tábor": added CampaignOpponent.aiCastleSkin/aiWallSkin: String fields (default "castle_player"/"wall_player"), applied per-location via `.map { it.copy(aiCastleSkin=..., aiWallSkin=...) }` on each location's opponents list (loc_goblins keeps castle_player_4/wall_goblin, now expressed as data instead of a GameScreen.kt id-prefix check). GameScreen.kt's opponentCastleResId/opponentWallResId now read `campaignOpponent?.aiCastleSkin/aiWallSkin` through castleSkinDrawable()/wallSkinDrawable() generically (falls back to defaults outside campaign), replacing the old `id?.startsWith("gob_")` hardcoded hack. castleSkinDrawable/wallSkinDrawable in GameBattlefield.kt extended with the 3 new skin ids (+ wall_goblin, which existed as an asset but had never been added to wallSkinDrawable's whitelist since the goblin location was wired through the hardcoded check instead).
## [2026-08-28] ingest | GameViewModel.kt startCampaignBattle(): Trpasličí hory (dwf_ opponent id prefix) now forces castle_background_winter as the battle background, same mechanism as Goblinský tábor's castle_background_goblin (id-prefix when-check). Other 2 locations still fall through to the default castle_background.
## [2026-08-28] ingest | Wired 11 new user-supplied assets for Trpasličí hory (matched by filename, "hory_X" -> the dwarf opponent whose title/role matches X): trpaslici_hory.png = location art (locationArtRes() in both CampaignLocationScreen.kt and CampaignMapScreen.kt, was art_opevneni placeholder). 10 individual portraits mapped to all 10 dwf_ opponents same as the goblin location's pattern: avatar changed from emoji/hammer_icon placeholders to "player_icon_10" (generic battle icon, no location has per-opponent battle icons except the goblin boss) + cardArt set to the matching hory_* roster-card illustration - dwf_miner->hory_hornik, dwf_carpenter->hory_tesar, dwf_guardian->hory_strazce, dwf_smith->hory_kovar, dwf_ranger->hory_ranger, dwf_warrior->hory_bojovnik, dwf_mage->hory_mag, dwf_general->hory_general, dwf_colossus->hory_kolos, dwf_thane->hory_thane (boss - only one image supplied, so unlike goblin_kral/goblin_kral_profil there's no separate close-up battle-icon variant for this boss, same avatar="player_icon_10" as the rest). FULL_ART_AVATARS set + avatarDrawableRes() in CampaignLocationScreen.kt extended with all 10 hory_* ids (CampaignResultScreen.kt's separate resolver untouched - it renders opponent.avatar, not cardArt, and player_icon_10 was already mapped there).
## [2026-08-28] fix | Fixed a small gap between the castle/wall structures and the bottom edge of the battlefield (background visible peeking through underneath), reported by the user. NewBattlefield's two NewCastleStructure calls (GameBattlefield.kt) both had `padding(bottom = 4.dp)` in addition to `.align(Alignment.BottomStart/End)` - removed the bottom padding so both castles sit flush against the battlefield's bottom edge (start/end padding kept, only vertical gap removed).
## [2026-08-28] fix | User reported ~3 opponent card art images in campaign have the head cropped at the card edge. Visually inspected all 22 goblin_*/hory_* full-art portraits (496x496 source, ~1.05:1 display crop window in OpponentCardArt - minimal crop margin, ~2.5% off each edge with the default centered Alignment.Center). Found 3 where the headdress/head touches the very top pixel row of the source art itself, so even that small centered crop clips it: goblin_troll (ear tips), goblin_valecny_nacelnik (skull-horn headdress), goblin_kral (crown spikes - also the boss's roster card art). Added TOP_ALIGNED_AVATARS set + alignment param on OpponentCardArt's Image (CampaignLocationScreen.kt): these 3 now crop with Alignment.TopCenter (crop comes only from the bottom) instead of the default Alignment.Center, so their tops render uncut. All other 19 portraits had comfortable margin at 1.05:1 crop and were left on the default centered alignment.
## [2026-08-28] fix | GameViewModel.kt's AI-turn combo loop (shared by offline/campaign/arena/roguelike - the AI plays a combo chain of multiple cards back-to-back within one turn) had only 450ms between each combo card's reveal - too fast to read what the AI just played. Bumped to 1000ms per user request. Single location (line ~1740, the `if (aiCard.isCombo || aiNextComboBoost) { ... delay(...) }` branch) - non-combo AI turns are unaffected (already end after 1 card, gated by the ~500-1000ms pre-turn delay).
## [2026-08-28] fix | Follow-up per user feedback: the 1000ms combo-card delay applied even after the LAST card of a combo chain (a card flagged isCombo=true that just happened to be the final one played, before AI ran out of things to do) - wasted 1s waiting for a "next card" reveal that never came. Restructured GameViewModel.kt's AI-turn while loop to peek ahead: right after playing a combo card, calls aiChooseAction() once to see what happens next, stores it in a new `pendingAiChoice` var consumed by the next loop iteration (avoids a second/inconsistent aiChooseAction() call - the function has a random +-2 scoring fuzz per its own doc comment, so calling it twice could give a different answer than the peek predicted). Delay is now 1000ms only if the peek says another card is coming (AiAction.Play), else 500ms (chain is actually ending - Wait/Discard).
## [2026-08-28] fix | CampaignLocationScreen.kt's location panel (left side) status text simplified from "$defeated / $total poraženo" to "$defeated / $total" per user request - the word was redundant/cluttering next to the numeric counter.
## [2026-08-28] fix | User reported dwarf-location (Trpasličí hory) opponent castles appear to "float" above the battlefield's bottom edge at full castle HP. Root cause: CastleTowerBlock (GameBattlefield.kt) renders the castle Image at a fixed 110x165dp box with contentScale=Fit and the DEFAULT alignment=Center. castle_player.png's aspect ratio (176x249, ~0.71) is close to the box's (0.67), so letterboxing was negligible - but castle_hory.png is 221x210 (~1.05, much wider/flatter), so Fit-scaling it to fit the box width leaves a large empty gap split evenly above AND below the image (Center alignment) instead of the castle sitting flush at the bottom like castle_player. Fixed by adding `alignment = Alignment.BottomCenter` to the Image - any letterbox gap from an aspect-ratio mismatch now goes to the top only, so the castle silhouette (whatever its native proportions) always sits flush with its box's bottom edge, composing correctly with the existing "emerge from ground" offsetY animation. Also fixed an unrelated stray `=` syntax error the user had left in CampaignData.kt line 403 (dwf_thane's deckCardCounts, "021" to 2=) while editing aiMaxWall - build was broken until this was removed.
## [2026-08-28] fix | Diagnosed user report: AI (Trpasličí Thane boss) discarded "Mohutná zeď" (BuildWall +15, cost 5 stone) despite having plenty of stone and wall room. Root cause found in AiEngine.kt's scoreEffect() BuildWall branch: the "plná hodnota" (full value, not urgent, not near cap) case returned a FLAT score of 5 regardless of fx.amount - unlike the sibling BuildCastle branch, which scales with `fx.amount / 5`. A cost-5 card giving +15 wall netted score ~5(effect) - 5(cost) = 0, well within the "bestScore <= 0" threshold that triggers a discard when hand is full (7/7) - so a genuinely efficient, high-value wall card got systematically undervalued relative to its cost and became prime discard bait, purely a scoring oversight rather than a logic bug (the discard/afford/hand-full branching itself was working as designed). Fixed by scaling the full-value case the same way as BuildCastle: `5 + fx.amount / 5` - a +15 wall card now scores 8 (net +3 after its 5-stone cost) instead of 0.
## [2026-08-28] fix | Same root-cause category, different case: user reported AI (Goblin Král) discarded "Vojenský rozkaz" (AddResource ATTACK +6, cost 3 magic, a combo setup card) despite it being a strong effect - AI only had 1/3 magic available, hand was full (7/7, now 6/7 post-discard), forcing a discard via the "can't afford anything in hand" branch, which picks the globally worst card in the WHOLE hand via bestDiscard() (not just unaffordable ones). AiEngine.kt's scoreEffect() had `is CardEffect.AddResource -> 3` - a FLAT score ignoring fx.amount entirely (unlike its own sibling CardEffect.AddResourceDelayed, which already scales via `fx.amount * 2` - the immediate version was the odd one out). A "+6 attack for 3 magic" card scored the same flat 3 as a "+1 attack" card, undervaluing it in bestDiscard()'s comparison against the rest of the hand and making it a discard candidate despite being a good combo-setup piece. Fixed to `3 + fx.amount / 2` (amount=1 unchanged at 3, amount=6 -> 6) - doesn't change whether the AI could AFFORD to play it that turn (it genuinely couldn't, 1<3 magic), but now correctly values it higher when forced to pick which card in a full hand to sacrifice, so it protects strong cards like this and discards something actually weak instead.
## [2026-08-28] ingest | Per user request: castle_background_goblin and the new castle_background_citadela joined RANDOM_BATTLE_BACKGROUNDS (GameBattlefield.kt) - previously castle_background_goblin was excluded from the general pool (campaign-only). Mirrored the same addition into server.js's BATTLE_BACKGROUNDS (online match backgrounds are server-picked, must stay in sync with the client pool - see the earlier server-authoritative-background fix). Also wired castle_background_citadela as the forced battle background for Temná citadela (cit_ opponent id prefix) in GameViewModel.kt's startCampaignBattle(), same when-branch pattern as goblin/dwarves. Dračí impérium (loc_dragon) is now the only location still on the default background. Also fixed another stray-character syntax error the user had left in CampaignData.kt line 398 (dwf_thane aiMaxWall = 50*, trailing asterisk) - same pattern as the two prior stray-typo fixes this session, build was broken until removed.
## [2026-08-28] ingest | Per user request: offline non-campaign matches started with a custom deck or super-random mode now get a random opponent castle/wall skin instead of the always-default castle_player/wall_player - normal mode is unchanged (stays default). Added RANDOM_OPPONENT_CASTLE_SKINS/RANDOM_OPPONENT_WALL_SKINS pools in GameBattlefield.kt (the same castle_player..castle_player_13 / wall_player..wall_player6 set the player can pick from in Profil - campaign-only skins like castle_hory/wall_goblin excluded) + randomOpponentCastleResId()/randomOpponentWallResId() pickers. GameViewModel.kt gained opponentCastleResId/opponentWallResId state (same pattern as battleBackgroundResId), set in restartGame() conditionally on `randomDeck || superRandom`. GameScreen.kt's NewBattlefield call now falls back to these instead of hardcoded "castle_player"/"wall_player" strings when campaignOpponent is null (campaign still takes priority via CampaignOpponent.aiCastleSkin/aiWallSkin, unaffected). Arena/roguelike battles were left untouched (out of scope - user specifically asked about the offline custom-deck/super-random mode picker).
## [2026-08-28] fix | User follow-up: "Vlastní balíček" and Roguelike still showed the default opponent skin, only Super Random worked. Two separate bugs: (1) MainActivity.kt's PLAY_MENU handlers both call restartGame(randomDeck = false, ...) - onOwnDeck passes randomDeck=false (it means something else entirely, unrelated to "custom deck" as a UI concept) and onSuperRandom ALSO passes randomDeck=false (only superRandom=true) - so the `if (randomDeck || superRandom)` gate added in the prior commit could only ever be satisfied by Super Random, never by "Vlastní balíček". Removed the gate entirely - restartGame() is only reachable from these two non-campaign PLAY_MENU entries, so it now always randomizes. (2) Roguelike sets activeCampaignOpponent to a procedurally-generated CampaignOpponent (generateRogueEnemy(), GameViewModel.kt) - GameScreen.kt's campaignOpponent-first fallback logic (from the same prior commit) therefore ALWAYS took the campaign branch for roguelike, reading aiCastleSkin/aiWallSkin off that generated opponent - which generateRogueEnemy() never set, so it silently defaulted to the CampaignOpponent data class's own "castle_player"/"wall_player" defaults, bypassing viewModel.opponentCastleResId/WallResId entirely regardless of what those held. Fixed by giving generateRogueEnemy() its own random skin per battle: added randomOpponentCastleSkinId()/randomOpponentWallSkinId() (String-returning siblings of the existing Int-returning randomOpponentCastleResId()/WallResId(), GameBattlefield.kt, both now built from a shared RANDOM_OPPONENT_CASTLE_SKIN_IDS/WALL_SKIN_IDS string pool) and set them on the generated CampaignOpponent's aiCastleSkin/aiWallSkin fields - flows through the normal per-opponent-skin campaign path, no GameScreen.kt changes needed this time.
